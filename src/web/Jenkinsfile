@Library('pipeline-library')
/*Map tasks = [failFast: false]

tasks['x86'] = { ->
    node {
        multiArchFlow('x86')
    }
}
tasks['arm'] = { ->
    node {
        multiArchFlow('arm')
    }
}*/

tech = "nodejs"

node {
    multiArchFlow('arm')
}

def multiArchFlow(String tech) {
    checkout(tech)
    
    downloadTooling(tech)

    build(tech)
}