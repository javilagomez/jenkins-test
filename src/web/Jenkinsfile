@Library('pipeline-library')

Map tasks = [failFast: false]

tech = "nodejs"

tasks['x86'] = { ->
    node {
        multiArchFlow('x86')
    }
}
tasks['arm'] = { ->
    node {
        multiArchFlow('arm')
    }
}

def multiArchFlow(String arch) {
    checkout(tech)
    
    downloadTooling(tech)

    build(tech)
}